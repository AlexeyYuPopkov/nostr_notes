import 'package:flutter/cupertino.dart';

class AccsTabContent extends StatelessWidget {
  const AccsTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('AccsTabContent'));
  }
}
